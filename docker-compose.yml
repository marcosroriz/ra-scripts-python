version: "3.9"

services:
  ra_update_linhas_kml:
    build: ./ra_update_linhas_kml
    container_name: ra_update_linhas_kml
    restart: unless-stopped
    env_file:
      - ./.env
    volumes:
      - ./logs:/home/grupo_fctufg/logs/

  rmtc_crawler_node:
    build: ./rmtc_crawler_node
    container_name: rmtc_crawler_node
    restart: unless-stopped
    env_file:
      - ./.env
    volumes:
      - ./logs:/home/grupo_fctufg/logs/
